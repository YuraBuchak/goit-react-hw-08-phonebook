import { Typography } from '@mui/material';

const NotFoundPage = () => {
  return (
    <div>
      <Typography
        variant="h2"
        component="div"
        sx={{ flexGrow: 1, color: '#4a4849' }}
      >
        Page NotFound
      </Typography>
    </div>
  );
};

export default NotFoundPage;
